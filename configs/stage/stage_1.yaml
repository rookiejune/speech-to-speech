name: stage_1
loaders:
  asr:
    weight: 0.5
    task_weights:
      asr: 1.0
  tts:
    weight: 0.5
    task_weights:
      tts: 1.0
batches_per_step: 2
trainable_groups:
  - semantic_audio_embedding
  - semantic_audio_adapter
  - acoustic_prompt
  - semantic_audio_output
  - acoustic_decoder
frozen_groups:
  - backbone
backbone_top_fraction: 0.0

