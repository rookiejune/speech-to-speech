# @package _global_
defaults:
  - /model: toy
  - /model/acoustic: none
  - /runtime: longcat_native
  - /data: toy
  - /pl_module: sft
  - /trainer: default
  - /logging: csv
  - override /stage: stage_1
  - override /parameter_policy: semantic_only
  - _self_

task: tts
run_name: longcat-decoupled-semantic-only
output_subdir: longcat-decoupled-semantic-only-smoke/${task}/${run_name}

data:
  sample_index: 0

runtime:
  device: cpu
  dtype: float32
  attn_implementation:

callbacks:
  task_sample:
    enabled: false
    every_n_steps: 1
  evaluation:
    enabled: false

train:
  seed: 0
  max_steps: 2

trainer:
  accelerator: cpu
  devices: 1
  precision: 32-true
  log_every_n_steps: 1
  enable_checkpointing: false
