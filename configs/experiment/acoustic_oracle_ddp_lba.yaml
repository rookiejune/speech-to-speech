# @package _global_
defaults:
  - /codec: longcat
  - /data: wmt19_tts_lba
  - /runtime: default
  - /init: codec
  - /optimizer: oracle
  - /flow: smoke
  - /train: smoke
  - /trainer: ddp
  - /logging: tensorboard
  - _self_

output_dir: ${oc.env:DYNAMIC_HOME,outputs}/train/speech-to-speech/005-codec-oracle-ddp-lba/${codec.name}/${init.name}

train:
  max_steps: -1
  normalize_features: false

runtime:
  audio_tokenizer:
