# @package _global_
defaults:
  - /codec: longcat
  - /data: wmt19_tts
  - /runtime: default
  - /init: codec
  - /optimizer: oracle
  - /flow: default
  - /train: overfit
  - /trainer: default
  - /logging: tensorboard
  - _self_

output_dir: ${oc.env:DYNAMIC_HOME,outputs}/train/speech-to-speech/005-codec-oracle/${codec.name}/${init.name}

runtime:
  audio_tokenizer:
