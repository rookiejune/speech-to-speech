# @package _global_
defaults:
  - /codec: longcat
  - /data: wmt19_tts
  - /init: codec
  - /train: overfit
  - /trainer: default
  - /logging: tensorboard
  - _self_

output_dir: ${oc.env:DYNAMIC_HOME,outputs}/train/speech-to-speech/005-codec-oracle/${codec.name}/${init.name}

token:
  layers: 2
  heads: 8
  feedforward_dim: 1024
  dropout: 0.0
