# @package _global_
defaults:
  - /stage@stage: stage_0
  - /callback/parameter_policy@callbacks.parameter_policy: full
  - /model: toy
  - /model/acoustic@model.acoustic: none
  - /runtime: longcat_full_sequence
  - /audio_sequence_layout: flattened
  - /datamodule/dataset@datamodule.dataset: toy
  - /pl_module: sft
  - /trainer: default
  - /logging: csv
  - _self_

task: tts
run_name: longcat-full-sequence-token
output_subdir: longcat-full-sequence-smoke/${task}/${run_name}

sample_index: 0

runtime:
  device: cpu
  dtype: float32
  attn_implementation:

callbacks:
  task_sample:
    enabled: false
    every_n_steps: 1
  evaluation:
    enabled: false

train:
  seed: 0
  max_steps: 2

trainer:
  accelerator: cpu
  devices: 1
  precision: 32-true
  log_every_n_steps: 1
  enable_checkpointing: false
