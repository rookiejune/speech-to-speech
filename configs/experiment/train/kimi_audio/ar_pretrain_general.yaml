# @package _global_
defaults:
  - /experiment/train/kimi_audio/ar_pretrain
  - _self_

# General-document text stream for the 7:1:1:1 phase-1 mixture.  The corpus
# path is deliberately required instead of falling back to WMT19 sentences.
text_datamodule:
  dataset:
    name: general
    path: ???
    format: jsonl
    encoding: utf-8
    language: en
  max_tokens: 4096
  pack_documents: true
