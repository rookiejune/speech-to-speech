# @package _global_
defaults:
  - /model: default
  - /runtime: unicodec
  - /data: wmt19_tts
  - /pl_module: sft
  - /trainer: default
  - /logging: tensorboard
  - _self_

task: tts
run_name: token
output_subdir: 005-codec-screening/unicodec/overfit

data:
  sample_index: 0

callbacks:
  task_sample:
    enabled: true
    every_n_steps: 1

train:
  seed: 0
  max_steps: 100

trainer:
  log_every_n_steps: 1
  enable_checkpointing: false
