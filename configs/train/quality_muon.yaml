max_steps: 5000000
learning_rate: 0.0001
adamw_learning_rate:
muon_learning_rate:
acoustic_loss_weight: 0.01
optimizer_preset: pretrain
optimizer: muon
weight_decay:
schedule: warmup_cosine
warmup_ratio: 0.01
stable_steps:
decay_steps:
min_lr_ratio: 0.1
seed: 0
device: auto
precision: bf16-mixed
