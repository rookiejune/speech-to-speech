oracle:
  sample_every_n_steps: 10000
  histogram_every_n_steps: 20
  save_audio: true
grad_norm:
  enabled: true
  every_n_steps: 100
nonfinite:
  enabled: true
checkpoint:
  filename: step-{step}
  save_last: true
  save_top_k: -1
  every_n_train_steps: 10000
