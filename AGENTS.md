# Agents

## 默认约定

- 开始前先阅读 `../AGENTS.md` 的默认约定，如果该文件存在；本文件只记录本仓库的补充约定。
- 涉及本地 Python、虚拟环境、测试、缓存或临时目录时，先阅读顶层 `../docs/dev-env.md`；
  不要使用系统或默认 `python` / `python3`。本仓库本地校验默认显式调用顶层约定的
  `py39` 环境；需要 Torch 2.12、Python 3.12-only 包或音频模型实验时再显式使用 `py312`。
